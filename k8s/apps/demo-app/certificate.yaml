apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-app
spec:
  secretName: demo-app-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  commonName: demo.local
  dnsNames:
    - demo.local